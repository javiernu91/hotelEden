---
import Picture from "astro/components/Picture.astro"

interface Props {
  class?: string
  altImage: string
  srcImage: ImageMetadata
  title: string
}

const {title, altImage, srcImage} = Astro.props
---

<article class=" min-w-[600px] snap-center bg-slate-400 relative rounded-md group">
  <Picture src={srcImage} alt={altImage} class="rounded-md group-hover:scale-105 hover:rounded-none ease-in-out duration-700" />
  <div class="flex flex-col absolute top-0 left-0 w-full h-full opacity-0 hover:opacity-100 hover:scale-105 ease-in-out duration-700 p-4 backdrop-blur-sm place-content-end text-gray-900 dark:text-white/90">
    <h2 class="font-semibold text-xl text-center ease-in-out duration-700 text-balance " >{title}</h2>    
  </div>
</article>