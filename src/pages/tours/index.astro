---
import Header from "../../components/Header.astro"
import Footer from "../../components/Footer.astro"
import Layout from "../../layouts/Layout.astro";
import TourCard from "../../components/TourCard.astro";

const tourPost = Object.values(import.meta.glob('../../content/tours/*.md', { eager: true }));

--- 

<Layout title="Planes en el PNN EL Cocuy">
  <Header/>


  <main class="relative w-full h-3/4 dark:bg-gray-950 bg-cover bg-no-repeat bg-center" >
    <div class="absolute top-1/4 w-full ">

      <h1 class="w-full text-center px-4 font-medium text-5xl dark:text-white/90 text-gray-900">Tours en el<strong class="text-green-500">PNN El Cocuy</strong> </h1>
      <h2 class="w-full text-center text-3xl font-semibold dark:text-white/90 text-gray-900">Nevado de Güicán, Cocuy y Chita</h2>
    </div>
  </main>

  <section class="max-w-7xl mx-auto px-4 dark:text-white/90 text-gray-900 py-16 text-center">
    <div class="mb-8">
      <h2 class="text-3xl font-semibold">Conoce el Nevado de Güicán de la Sierra en el PNN El Cocuy</h2>
      <h3 class="text-2xl font-semibold">Recorre un paraíso natural y descubre nuestros sitios turísticos</h3>

    </div>

    <p class="text-pretty leading-normal my-4">Descubr el <strong>Nevado de Güicán de la Sierra</strong> y el PNN El cocuy con una variedad de experiencias únicas que están todas a tu alcance. Explora nuestros planes seleccionados de tours por los mejores destinos en tu visita al <strong>Nevado de Güicán en el Parque Nacional Natural El Cocuy</strong>.</p>
    <p class="text-pretty leading-normal my-4">Haz que tu visita a Güicán sea inolvidable, dejanos acompañar tu experiencia.</p>
  </section>

  <section class="max-w-7xl mx-auto">
    <ul class="grid grid-cols-4" >
      {
        tourPost.map((post) => (
          <TourCard
            title={post.frontmatter.title}
            imgSrc={post.frontmatter.imgSrc}
            imgAlt={post.frontmatter.imgAlt}    
          />
        ))
      }
    </ul>
  </section>

  <Footer/>
</Layout>


<style>

  main {
    background-image: url(/public/ritacuba.jpg);
  }
</style>
